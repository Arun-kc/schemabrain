# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
# Re-include the dashboard's frontend `lib/` directory — without
# this negation, the generic `lib/` pattern above silently swallows
# `web/lib/{api,cn}.ts` + `web/lib/types/*.ts` (the TypeScript
# mirrors of the Python charter). Symptom: tests pass locally, fail
# in CI with FileNotFoundError on the TS files. Discovered after
# PR #125 CI 26448572653.
!web/lib/

# Dashboard static export — populated by `pnpm --dir web build` at
# release-time CI from the `web/` source; never committed. The
# `.gitkeep` + README.md sentinels keep the directory tracked so the
# wheel artifacts glob in pyproject.toml finds a real path even when
# the build hasn't been run yet.
schemabrain/dashboard/static/*
!schemabrain/dashboard/static/.gitkeep
!schemabrain/dashboard/static/README.md

parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments
.env
.env.*
!.env.example
.venv
.venv*/
env/
venv/
ENV/
env.bak/
venv.bak/

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Type checking
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/

# Ruff
.ruff_cache/

# SchemaBrain local artifacts
schemabrain.db
schemabrain.db-journal
schemabrain.db-wal
schemabrain.db-shm
schemabrain-*.db
*.schemabrain.db
.schemabrain/
.fastembed_cache/

# Benchmark / cache artifacts not covered above
.benchmarks/

# Logs
*.log
logs/

# Claude
.claude/

# Agent review artifacts (PR-review rotation outputs)
.review/

# Internal-only docs (PR plans, smoke notes, audits, backlogs)
# Kept locally for engineering reference; not shipped to the public repo.
docs/internal/
schemabrain-prerelease.db*

# Dashboard static export — populated by `pnpm --dir web build` at
# release-time CI from the `web/` source; never committed. The
# `.gitkeep` + README.md sentinels keep the directory tracked so the
# wheel artifacts glob in pyproject.toml finds a real path even when
# the build hasn't been run yet.
schemabrain/dashboard/static/*
!schemabrain/dashboard/static/.gitkeep
!schemabrain/dashboard/static/README.md
