version: 1
name: user_count
description: |
  Number of user accounts. Demonstrates PII-aware refusal: grouping by
  a credential column is refused, while grouping by a public column like
  `role` is allowed.
entity: user
measure:
  agg: count
  column: id
